# GWS-STNet Financial Metabolomics — Python dependencies
# Tested with Python 3.10+

torch>=2.1.0
torchvision>=0.16.0
numpy>=1.24.0
scipy>=1.11.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
scikit-learn>=1.3.0
tqdm>=4.65.0

# Transfer entropy estimation
# (KSG estimator — install from source if not on PyPI)
jpype1>=1.4.0       # Java bridge for JIDT (optional, for validation)

# Statistical tests
statsmodels>=0.14.0

# Reproducibility
zenodo-get>=1.3.4   # for downloading Zenodo archive
